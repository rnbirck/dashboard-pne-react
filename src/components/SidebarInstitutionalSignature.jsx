export function SidebarInstitutionalSignature({ compact = false }) {
  return (
    <div className={`sidebar-institutional-signature${compact ? ' sidebar-institutional-signature--compact' : ''}`}>
      <p>Observatório da Educação — SESI-RS</p>
    </div>
  )
}
